# Feature Implementation

## Feature Details
- **ID**: inter-set-rest-timer-1766605069177
- **Title**: Add an automatic rest timer that triggers between workout sets
- **Category**: User Experience

## Description
Add an automatic rest timer that triggers between workout sets

## Verification Steps
1. Modify ActiveWorkoutScreen in expo/app/workout/[id].tsx
2. Add state for 'rest' mode vs 'active' mode
3. Trigger countdown overlay when a set is marked as completed

## Instructions
1. **Explore**: Review the existing codebase structure and patterns
2. **Plan**: Design your implementation approach
3. **Implement**: Write clean, production-quality code
4. **Test**: Verify the feature works as expected
5. **Update**: Mark feature as "passes": true in feature_list.json
6. **Commit**: Use message "feat(User Experience): Add an automatic rest timer that triggers between workout sets"

## Important
- Implement ONLY this specific feature (ID: inter-set-rest-timer-1766605069177)
- Do NOT modify features already marked as passes: true
- Follow existing code patterns and conventions
- Maintain backward compatibility
- Update feature_list.json to set "passes": true when complete