# Feature Implementation

## Feature Details
- **ID**: workout-exercise-builder-1767018234680
- **Title**: Create a drag-and-drop interface within the Workout form to assign, order, and configure exercises
- **Category**: Admin

## Description
Create a drag-and-drop interface within the Workout form to assign, order, and configure exercises

## Verification Steps
1. Create backend endpoints POST/PUT /api/workouts/:id/exercises to handle batch updates of the workout_exercises join table
2. Update vitereact/src/pages/admin/WorkoutForm.tsx to include an 'Exercise Picker' modal
3. Implement a list component in the form to manage order_index, default_sets, and default_reps for selected exercises
4. Add validation to ensure a workout has at least one exercise before publishing

## Instructions
1. **Explore**: Review the existing codebase structure and patterns
2. **Plan**: Design your implementation approach
3. **Implement**: Write clean, production-quality code
4. **Test**: Verify the feature works as expected
5. **Update**: Mark feature as "passes": true in feature_list.json
6. **Commit**: Use message "feat(Admin): Create a drag-and-drop interface within the Workout form to assign, order, and configure exercises"

## Important
- Implement ONLY this specific feature (ID: workout-exercise-builder-1767018234680)
- Do NOT modify features already marked as passes: true
- Follow existing code patterns and conventions
- Maintain backward compatibility
- Update feature_list.json to set "passes": true when complete